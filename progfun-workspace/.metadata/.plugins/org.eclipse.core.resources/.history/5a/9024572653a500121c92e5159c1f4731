package common

object SheetCommon {
  val per = 100                                   //> per  : Int = 100
  val weeks = 7                                   //> weeks  : Int = 7
  
  def total(weekTotal: Array[Double]) : Double = {
			for (wt : weekTotal) {
			
			}
		
		  x
  }                                               //> total: (x: Double)Double
}