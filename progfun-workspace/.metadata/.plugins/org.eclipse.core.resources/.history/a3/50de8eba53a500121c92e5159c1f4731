package common

object SheetCommon {
  val per = 100
  val weeks = 7
  
  def total(weekTotal: Array[Double]): Double = {
			def sum(index: Int, acc: Double): Double = {
				if (index > 7) 0
				else weekTotal(index) + sum(index + 1)
			}
			
			
		
		  x
  }
}